# assets.AssetLibrary

``` python
AssetLibrary()
```

Locates a `.blend` asset library on disk at runtime.

## Methods

| Name | Description |
|----|----|
| [path](#nodebpy.assets.AssetLibrary.path) | Absolute path to the `.blend` file. |

### path

``` python
path()
```

Absolute path to the `.blend` file.
